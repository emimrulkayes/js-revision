/* Array for Loop */
var numbers = [43, 45, 76, 67, 89, 99, 34, 42];
for (var i = 0; i < numbers.length; i++) {
    var num = numbers[i];
    console.log(num);
}

/* Example two  */
const products = ['Aloo', 'Onion', 'Appel', 'Komola', 'Canacur', 'Busikuit'];
for (let i = 0; i < products.length; i++) {
    let product = products[i];
    console.log(product);
}